ARG BASE_IMAGE='DEPENDENCIES_BASE_IMAGE_IS_REQUIRED'

ARG BUILDPLATFORM='BUILDPLATFORM_IS_REQUIRED'

FROM  --platform=$BUILDPLATFORM ${BASE_IMAGE}

COPY docker/dependencies/fs-root /

RUN . /root/.bashrc \
    && pnpm install
