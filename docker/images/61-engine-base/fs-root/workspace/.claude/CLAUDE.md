# Dinghy IAC Environment

## Default Skill

When the user asks to create or modify infrastructure, invoke the `dinghy-iac` skill.

## Verify Changes

Run `dinghy render` then `dinghy check` before committing. Both must pass.
