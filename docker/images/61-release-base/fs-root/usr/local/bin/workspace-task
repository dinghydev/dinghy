#!/bin/bash -e

# Get the first argument as task_name
task_name="$1"
# Shift to remove the first argument, leaving remaining args in $@
shift

deno run \
  --config /diac/workspace/deno.jsonc \
  --lock /diac/workspace/deno.lock \
  --allow-all \
   --cached-only \
  /diac/workspace/tasks/${task_name}/index.ts \
  "$@"
