#!/bin/bash

# Make the script exit on any error
set -e
cd /reactiac/workspace

# Get the first argument as task_name
task_name="$1"
# Shift to remove the first argument, leaving remaining args in $@
shift

deno task \
  --config /reactiac/workspace/deno.jsonc \
  --frozen=true \
  ${task_name} $@
