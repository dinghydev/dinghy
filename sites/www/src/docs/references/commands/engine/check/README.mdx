---
edit_url: https://github.com/dinghydev/dinghy/blob/main/engine/src/commands/check.ts
---
# check
Run static code analysis, available checks: fmt, lint, type, git
## Usage
```sh
$ dinghy check [CHECK] [options]
```
## Arguments
| Name            | Description                     |
| ---------------- | ----------------------------- |
| `check` |The checks to run  |
## Options
| Name            | Description           | Default                   |
| ---------------- | --------------- | ----------------------------- |
| `--fmtCmd=<value>` |The command to run for fmt check | `deno fmt` |
| `--lintCmd=<value>` |The command to run for lint check | `deno lint` |
| `--typeCmd=<value>` |The command to run for type check | `deno check` |
| `--gitCmd=<value>` |The command to run for git check | `git diff` |
| `[--checks=<value>]` |The checks to run | `fmt,lint,type,git` |